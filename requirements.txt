# bh_sensing_analysis — pip-installable mirror of the deps (NF-3 reproducibility).
# SOURCE OF TRUTH is pyproject.toml + uv.lock; prefer `uv sync`. This file is a
# fallback for pip-only environments and is kept in lockstep with pyproject.toml.
# matplotlib / statsmodels / Bland-Altman deps intentionally EXCLUDED (PLAN §8 cut #3).
numpy==2.4.6
onnx==1.21.0
onnxruntime==1.26.0
opencv-python-headless==4.13.0.92
duckdb==1.5.3
pandas==3.0.3
streamlit==1.58.0
plotly==6.8.0
